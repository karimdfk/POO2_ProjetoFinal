package com.example.projetofinalpoo2.observer;

public interface Observable { //interface Observable que é implementada pela entidade tarefa que é o objeto que desejamos observar para ver se tem alguma atualização
    public void notifica();
}
